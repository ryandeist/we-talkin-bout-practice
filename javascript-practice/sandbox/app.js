unflattenedArray = [{a: 1, b: {c: 2, d: 3}}]

function flatten(value) {
    if (typeof value !== 'object' || value === n)
    if 
    arr.forEach((val) => {
        console.log(val);
    })
}

flatten(unflattenedArray);

